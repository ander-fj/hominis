import { useState } from 'react';
import { BellRing, ShieldCheck, Mail, MessageCircle } from 'lucide-react';
import { Button } from '@/components/ui';
import { pushService } from '@/modules/notifications/services/push.service';
export function NotificationPreferencesPage(){const [message,setMessage]=useState(''); const [busy,setBusy]=useState(false); const state=pushService.state(); const enable=async()=>{setBusy(true);setMessage('');try{await pushService.enable();setMessage('Notificações push ativadas neste dispositivo.');}catch(e){setMessage(e instanceof Error?e.message:'Não foi possível ativar.');}finally{setBusy(false);}}; return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Canais de notificação</h1><p className="text-sm text-slate-500">Configure como os alertas importantes chegam aos usuários.</p></div><div className="grid gap-4 md:grid-cols-3"><div className="rounded-xl border bg-white p-5"><BellRing className="mb-3"/><h2 className="font-semibold">Push no aplicativo</h2><p className="mt-2 text-sm text-slate-500">Receba alertas mesmo fora da tela do sistema.</p><p className="mt-3 text-xs">Status: {state.permission}</p><Button className="mt-4" disabled={busy||state.permission==='granted'} onClick={enable}>{busy?'Ativando...':state.permission==='granted'?'Ativado':'Ativar push'}</Button></div><div className="rounded-xl border bg-white p-5"><Mail className="mb-3"/><h2 className="font-semibold">E-mail</h2><p className="mt-2 text-sm text-slate-500">Estrutura preparada para integração via Edge Function, Resend ou SMTP.</p><span className="mt-4 inline-block rounded bg-slate-100 px-2 py-1 text-xs">Próxima integração</span></div><div className="rounded-xl border bg-white p-5"><MessageCircle className="mb-3"/><h2 className="font-semibold">WhatsApp</h2><p className="mt-2 text-sm text-slate-500">Estrutura preparada para provedores oficiais via webhook seguro.</p><span className="mt-4 inline-block rounded bg-slate-100 px-2 py-1 text-xs">Próxima integração</span></div></div>{message&&<div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-700"><ShieldCheck className="mr-2 inline" size={16}/>{message}</div>}</div>}
